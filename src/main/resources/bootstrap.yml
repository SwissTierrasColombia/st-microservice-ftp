spring:
  application:
    name: st-microservice-ftp
  profiles:
    active: "develop"
  cloud:
    config:
      enabled: true
      
---
spring:
  profiles: develop
  cloud:
    config:
      failFast: true
      uri: http://localhost:8889
      
---
spring:
  profiles: production
  cloud:
    config:
      failFast: true
      uri: http://3.15.244.160:8890
